apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}-archlinux-pvc
  labels:
    {{- include "linux-repository-mirror.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 120Gi
  storageClassName: {{ .Values.mirror.archlinux.pv.storageClassName }}
  selector:
    matchLabels:
      name: {{ .Values.mirror.archlinux.pv.name }}

---

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}-manjaro-pvc
  labels:
    {{- include "linux-repository-mirror.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 350Gi
  storageClassName: {{ .Values.mirror.manjaro.pv.storageClassName }}
  selector:
    matchLabels:
      name: {{ .Values.mirror.manjaro.pv.name }}

---

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}-ubuntu-pvc
  labels:
    {{- include "linux-repository-mirror.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 450Gi
  storageClassName: {{ .Values.mirror.ubuntu.pv.storageClassName }}
  selector:
    matchLabels:
      name: {{ .Values.mirror.ubuntu.pv.name }}

---

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}-debian-pvc
  labels:
    {{- include "linux-repository-mirror.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 400Gi
  storageClassName: {{ .Values.mirror.debian.pv.storageClassName }}
  selector:
    matchLabels:
      name: {{ .Values.mirror.debian.pv.name }}

---

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}-raspbian-pvc
  labels:
    {{- include "linux-repository-mirror.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 250Gi
  storageClassName: {{ .Values.mirror.raspbian.pv.storageClassName }}
  selector:
    matchLabels:
      name: {{ .Values.mirror.raspbian.pv.name }}

---

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}-gentoo-pvc
  labels:
    {{- include "linux-repository-mirror.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 850Gi
  storageClassName: {{ .Values.mirror.gentoo.pv.storageClassName }}
  selector:
    matchLabels:
      name: {{ .Values.mirror.gentoo.pv.name }}
